import assert from "node:assert/strict";
import test from "node:test";

// Replace this fail-closed sentinel with behavior tests that import the
// candidate from process.cwd(). Keep control-owned helpers relative to here.
test("REPLACE_ME: canonical API contract has been implemented", () => {
  assert.fail("contract_not_customized");
});
